{{#if user}}
    <h1 id="txtAlignCenter" class="lead">My Lists</h1>
    <table class="table card">
        {{#each user.lists}}
            <tr>
                <!--<th>-->
                    <!--<a class="btn btn-outline-warning" href="#" role="button"><span class="fa fa-film"></span></a>-->
                <!--</th>-->
                <td>
                    <a class="btn btn-default" href="#" role="button"><span class="fa fa-film"></span>{{name}}</a>
                    <!--<a href="/movies/{{id}}">{{originalTitle}}</a>-->
                </td>
            </tr>
        {{/each}}
    </table>
{{/if}}